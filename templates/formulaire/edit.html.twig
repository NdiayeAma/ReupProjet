{% extends 'base.html.twig' %}

{% block title %}Edit Formulaire{% endblock %}

{% block body %}
    <h1>Edit Formulaire</h1>

    {{ include('formulaire/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a href="{{ path('app_formulaire_index') }}" class="btn btn-outline-primary mx-1">Retour</a>

    {{ include('formulaire/_delete_form.html.twig') }}
{% endblock %}
